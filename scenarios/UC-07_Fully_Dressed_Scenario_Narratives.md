# UC-07 Fully Dressed Scenario Narratives

## Main Success Scenario (Happy Path) — Submit Paper Manuscript

The primary actor initiates UC-07 to accomplish the goal: submit paper manuscript. The system guides the actor through the required steps and performs validations and persistence as needed. When all required conditions are satisfied, the system completes the action successfully and informs the actor.

---

## Alternative Path 4a

The actor performs the use case, but the condition in extension 4a occurs. The system rejects or pauses the workflow, displays an appropriate error/status message, and allows the actor to correct the issue or try again later.

---

## Alternative Path 4b

The actor performs the use case, but the condition in extension 4b occurs. The system rejects or pauses the workflow, displays an appropriate error/status message, and allows the actor to correct the issue or try again later.
